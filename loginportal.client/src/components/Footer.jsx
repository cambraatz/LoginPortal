const Footer = (props) => {
    return(
        <div id={props.id}>
            <p id="footer_text">Developed by Transportation Computer Support, LLC.</p>
        </div>
    )
}

export default Footer;